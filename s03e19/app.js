// javascript is a dinamically typed language. this means that
// a js engine determines the type of a variable at runtime.

var a = 10;
a = 'ten';
a = true;

if (a === true) {
    console.log('hello');
}