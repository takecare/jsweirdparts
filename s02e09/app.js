// base/global execution context: two objects are created - 'global' and 'this'
// on a browser, 'this' (on the global levet) points to the window object
// (same as 'window' object), which is the global object.

// both 'a' and 'b' are created at the global level here
var a = 'Hello World!';

function b() {
    
}